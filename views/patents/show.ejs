<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patent Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="/tshow.css">
</head>
<body>
    <div class="container">
        <h3><%= patent.facultyName %></h3>
        <p><b>Patent Title:</b> <%= patent.patentTitle %></p>
        <p><b>Patent Number:</b> <%= patent.patentNumber %></p>
        <p><b>Date of Award:</b> <%= new Date(patent.dateOfAward).toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }) %></p>
        <p><b>Patent Granting Body:</b> <%= patent.grantingBody %></p>
        <p><b>Patent Status (Published/Granted):</b> <%= patent.status %></p>

        <p><b>Certificate:</b>
            <img src="<%=patent.certificate.url%>" class="card-img show-img" alt="certificate">
        </p>
   
       
        <div class="btns">
            <a href="/index/patents/<%= patent._id %>/edit" class="btn btn-primary">Edit</a>
            <form method="post" action="/index/patents/<%= patent._id %>?_method=Delete">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>
        </div>
   
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
